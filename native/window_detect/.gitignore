# Rust build output
/target

# Built artifact deployed into the demo (regenerated by `cargo build` + copy)
/demo/window_detect.dylib

# Godot editor import cache (the tracked file is .godot/extension_list.cfg only)
/demo/.godot/*
!/demo/.godot/extension_list.cfg
